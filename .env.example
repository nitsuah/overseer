# Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================
# AGENT INSTRUCTIONS:
# This template shows required environment variables.
#
# When generating:
# 1. Analyze package.json, config files, and codebase for actual env var usage
# 2. Check for references to process.env.* in the code
# 3. Include all API keys, database URLs, and secrets mentioned
# 4. Group related variables together with comments
# 5. Provide example values or placeholders (never real credentials)
# 6. Mark required vs optional variables
# 7. Include links to documentation for obtaining API keys
# ============================================

# Application
NODE_ENV=development # 'development' or 'production'
PORT=3000 # Port the application listens on

# Database
DATABASE_URL=postgresql://user:password@host:port/database # Connection string for the database

# Authentication & Security
AUTH_SECRET=your_secret_key # Secret key used for signing JWTs or other authentication tokens.  Must be a strong, randomly generated string.
# Optional: Session secret if using sessions
SESSION_SECRET=another_strong_secret # Used for session management

# External APIs
GITHUB_API_KEY=your_github_api_key # API key for the GitHub API.  See [GitHub API documentation](TODO: GITHUB_API_DOCS_URL) for instructions.

# Runtime Configuration
LOG_LEVEL=info # Log level: 'debug', 'info', 'warn', 'error', 'fatal'